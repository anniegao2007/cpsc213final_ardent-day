<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <link href="/static/main.css" rel="stylesheet">
    
    <div class="topnav">
        <logo><img src="https://i.imgur.com/IuyFTt8.png"  height="80" width="213"> </logo>
        {{#if currentUser}}
        <a href="/">Your Classes</a>
        {{/if}}
        {{#if currentUser}}
        <a href="/user/logout" class="logot" style = "text-align: right;">Logout</a>
        <a style="float: right; background-color: #1F487E; color: white;">Welcome, {{currentUser.name}}!</a>
        {{/if}}
        {{#unless currentUser}}
        <login> Already Registered?
                <form action="/user/login" method="POST" class="login">
                <input type="text" name="email" placeholder="email"required>
                <input type="password" name="password" placeholder = "password" required>
                <input type="submit" value="Login">
                </form>
        </login>
        {{/unless}}
        
    </div>

</head>
<body>
    {{{body}}}
</body>
</html>
